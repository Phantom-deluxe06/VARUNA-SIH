fastapi
uvicorn
pydantic

# --- Satellite EO & Raster Engine (lazily-imported scientific stack) -----------
numpy
xarray
netCDF4
scikit-learn
joblib

# --- Realtime / AIS ingestion -------------------------------------------------
websockets>=12.0
pyais>=2.7

# --- WhatsApp bot + web form parsing -----------------------------------------
flask
twilio
python-multipart

# --- Optional: Copernicus Marine as PRIMARY chlorophyll source ---------------
# Needs a free CMEMS account (COPERNICUSMARINE_SERVICE_USERNAME / _PASSWORD).
# Dormant without it -- NOAA ERDDAP remains the working key-less source.
#   pip install copernicusmarine
